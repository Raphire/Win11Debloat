<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Restore Backup"
        Width="500"
        SizeToContent="Height"
        MaxHeight="560"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False">

    <Border Style="{DynamicResource ModalCardBorderStyle}">

        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" x:Name="TitleBar" Style="{DynamicResource ModalTitleBarStyle}">
                <TextBlock Margin="20,0,0,0"
                           Style="{DynamicResource ModalTitleTextStyle}"
                           Text="Restore registry backup"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0">
                <Grid Margin="20,4,20,8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid x:Name="IntroInfoPanel" Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   TextWrapping="Wrap"
                                   FontSize="14"
                                   LineHeight="20"
                                   Foreground="{DynamicResource FgColor}"
                                   Text="This will revert any registry changes made by Win11Debloat to their previous state. You can review the changes after selecting a backup file. Apps will need to be reinstalled manually."/>

                        <TextBlock Grid.Row="1"
                                   Margin="0,12,0,12"
                                   TextWrapping="Wrap"
                                   FontSize="13"
                                   LineHeight="18"
                                   Foreground="{DynamicResource FgColor}"
                                   Opacity="0.75"
                                   Text="Please note: Only use backup files generated by this tool."/>
                    </Grid>

                    <Grid x:Name="OverviewPanel" Grid.Row="2" Margin="0" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="File:"
                                       Style="{DynamicResource ModalInfoLabelTextStyle}"/>
                            <TextBlock x:Name="BackupFileText"
                                       Grid.Row="0" Grid.Column="1"
                                       Text=""
                                       Style="{DynamicResource ModalInfoValueTextStyle}"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,8"/>

                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="Created:"
                                       Style="{DynamicResource ModalInfoLabelTextStyle}"/>
                            <TextBlock x:Name="BackupCreatedText"
                                       Grid.Row="1" Grid.Column="1"
                                       Text=""
                                       Style="{DynamicResource ModalInfoValueTextStyle}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="Target:"
                                       Style="{DynamicResource ModalInfoLabelTextStyle}"
                                       Margin="0,0,16,0"/>
                            <TextBlock x:Name="BackupTargetText"
                                       Grid.Row="2" Grid.Column="1"
                                       Text=""
                                       Style="{DynamicResource ModalInfoValueTextStyle}"
                                       Margin="0"/>
                        </Grid>

                        <Border Grid.Row="1"
                                Height="1"
                                Background="{DynamicResource BorderColor}"
                                Margin="0,0,0,12"/>

                        <Grid Grid.Row="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="The following changes will be reverted:"
                                       FontSize="13"
                                       Foreground="{DynamicResource FgColor}"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>

                            <ItemsControl x:Name="FeaturesItemsControl" Grid.Row="1">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" IsItemsHost="True"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayText}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource FgColor}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,6"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>

                        <Border x:Name="NonRevertibleSeparator" Grid.Row="3"
                                Height="1"
                                Background="{DynamicResource BorderColor}"
                                Margin="0,12,0,12"
                                Visibility="Collapsed"/>

                        <Grid x:Name="NonRevertiblePanel" Grid.Row="4" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="The following changes cannot be automatically reverted:"
                                       FontSize="13"
                                       Foreground="{DynamicResource FgColor}"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>

                            <ItemsControl x:Name="NonRevertibleFeaturesItemsControl" Grid.Row="1">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" IsItemsHost="True"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayText}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource FgColor}"
                                                   Opacity="0.85"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,6"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock x:Name="NonRevertibleWikiLink"
                                       Grid.Row="2"
                                       Margin="0,12,0,0"
                                       Text="Visit our wiki for instructions on how to revert the remaining changes."
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Style="{DynamicResource HyperlinkStyle}"
                                       TextWrapping="Wrap"/>
                        </Grid>
                    </Grid>
                </Grid>
            </ScrollViewer>

                <Border Grid.Row="2" Style="{DynamicResource ModalFooterBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="SelectFileBtn"
                            Grid.Column="0"
                            Content="Select backup file"
                            Style="{DynamicResource ModalPrimaryStretchedButtonStyle}"/>
                    <Button x:Name="RestoreBtn"
                            Grid.Column="0"
                            Content="Restore from backup"
                            Visibility="Collapsed"
                            Style="{DynamicResource ModalPrimaryStretchedButtonStyle}"/>
                    <Button x:Name="CancelBtn"
                            Grid.Column="1"
                            Content="Cancel"
                            Style="{DynamicResource ModalSecondaryStretchedButtonStyle}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
